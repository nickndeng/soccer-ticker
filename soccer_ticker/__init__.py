"""Soccer Ticker — live soccer scores in your system tray / menu bar.

Cross-platform: a shared, toolkit-free `core` (ESPN client + match model)
with per-platform front-ends (GTK AppIndicator on Linux, rumps on macOS).
"""
__version__ = "0.2.0"
